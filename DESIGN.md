# piatoss.log — DESIGN.md

> 블로그 디자인의 단일 기준(source of truth).

## 1. 역할

piatoss.xyz가 드림코어·위어드코어 세계를 보여 주는 포트폴리오라면, piatoss.log는 그 세계 안의 **읽는 방**이다. 분위기는 이어지되 글을 찾고 읽는 속도를 방해하지 않는다.

- 내용: 지갑, Zcash, 개발, AI, 회고를 포함한 글
- 구조: 기존 블로그의 컴팩트한 헤더, 얕은 배너, 44rem 본문, 글 목록
- 정서: 비 온 뒤의 한국 학교와 골목, 잘못 이어진 어린 시절의 기억, 오래된 MiniDV의 색번짐

## 2. 단일 테마

라이트/다크 또는 밤/먹 모드는 두지 않는다. 모든 화면은 하나의 어두운 청록·앰버 팔레트를 쓴다.

```css
--bg: #080d12;
--surface: #0d1419;
--text: #e9e5dc;
--text-muted: #a6afad;
--line: rgba(167, 190, 188, 0.13);
--accent: #d68c43;
```

- 앰버는 오래된 가로등과 실내 불빛처럼 제한적으로 쓴다.
- 네온, 과한 글리치, 공포 연출은 쓰지 않는다.
- 드림코어의 낯섦은 장식 UI가 아니라 이미지 속 빈 골목과 거리감으로 만든다.

## 3. 이미지 슬롯

큰 장면 이미지는 두 곳에만 둔다.

| 슬롯 | 파일 | 화면 높이 |
|---|---|---|
| 홈 상단 배너 | `/public/dreamcore/hero-banner.webp` | 데스크탑 200–285px, 모바일 190px |
| 푸터 스트립 | `/public/dreamcore/footer-road.webp` | 데스크탑 150–230px, 모바일 160px |

- 두 이미지는 2172×724의 3:1 가로 파노라마다.
- 배너의 운동장과 푸터의 골목은 같은 학교와 독립된 교실 문으로 이어지는 한 장면이다.
- 배너는 벽 없이 서 있는 문과 같은 방향을 보는 그네, 푸터는 문을 향해 반복되는 파란 교실 의자를 핵심 단서로 쓴다.
- 텍스트를 덮는 구간에만 어두운 그라디언트를 둔다.
- 풀스크린 히어로, 별도 영상 프레임, 타임코드 패널은 만들지 않는다.

## 4. 레이아웃

- 홈: 얕은 배너 → 새로 쓴 글 → 분리된 티스토리 아카이브 미리보기
- 새 글(`/writing`): `tistoryId`가 없는 이 블로그 고유 글만 모아 보는 목록
- 아카이브: `tistoryId`가 있는 이관 글만 연도별로 표시
- 카테고리: 단순 그리드와 해당 글 목록
- 글 본문: 44rem 읽기 폭, 세리프 제목, 산세리프 기술 본문
- 에세이(`생각 정리`): 본문까지 Noto Serif KR과 넉넉한 행간
- 푸터: 이미지 스트립 뒤 짧은 사이트 정보

## 5. 타이포그래피와 글 끝 오너먼트

- `Diphylleia`는 헤더 워드마크, 홈 배너 제목, 푸터 태그라인에만 사용한다.
- 본문, 네비게이션, 메타데이터는 기존 Outfit·Pretendard·Noto Serif KR 조합을 유지한다.
- 글 끝에는 `/public/dreamcore/post-diary.webp`를 사용한다. 표지 안의 빈 놀이터가 배너·푸터 세계와 이어지는 장면이다.

## 6. 가드레일

- 테마 토글, `data-theme`, 테마용 `localStorage`를 추가하지 않는다.
- 홈 히어로를 뷰포트 높이로 키우지 않는다.
- 새로 쓴 글과 `tistoryId`가 있는 이관 글을 같은 목록이나 이전·다음 탐색에 섞지 않는다.
- 배너와 푸터 외에 큰 이미지 프레임을 반복하지 않는다.
- 카드와 효과보다 글 제목, 날짜, 카테고리의 탐색성을 우선한다.
- UI 변경은 데스크탑과 모바일 실제 렌더에서 확인한 뒤 완료로 판단한다.
