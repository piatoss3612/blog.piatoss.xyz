# piatoss.log — DESIGN.md

> 블로그 디자인의 단일 기준(source of truth).

## 1. 역할

piatoss.xyz가 세계라면 piatoss.log는 그 세계 안의 **읽는 방**이다. 색과 온도만 상속하고 장식은 억제한다. 글을 찾고 읽는 속도를 방해하지 않는다.

- 이름: piatoss.log — "장부(ledger)에 기록을 남긴다"는 메타포
- 내용: 지갑, Zcash, 개발, AI, 회고. 티스토리에서 옮긴 209편을 품되 앞으로의 글은 에세이 비중이 커진다
- 정서: 야간 숲의 랜턴 불빛, 픽셀 디오라마

## 2. 단일 테마

밤 테마 하나만 쓴다. 라이트/다크 토글도, 먹(墨) 테마도 두지 않는다.

```css
--sl-bg: oklch(0.1 0.01 60);          /* warm off-black */
--sl-bg-raised: oklch(0.13 0.012 60);
--sl-accent: oklch(0.65 0.17 55);     /* 랜턴 앰버 */
--sl-text: oklch(0.88 0.02 80);
--sl-text-muted: oklch(0.66 0.025 76);
--sl-card-border: rgba(219, 176, 124, 0.28);
```

- 앰버는 랜턴과 실내 불빛처럼 제한적으로 쓴다.
- 네온, 과한 글리치, 공포 연출은 쓰지 않는다.
- **텍스트 색은 배경 대비 4.5:1 이상.** 날짜·잔글씨·카테고리 칩처럼 보조적인 것도 예외가 아니다. 배경이 거의 검정이라 `oklch` 밝기를 0.6 아래로 내리면 미달한다 — 실제로 `--sl-muted`가 1.82:1까지 떨어져 날짜가 안 보인 적이 있다.

## 3. 아트 슬롯

| 슬롯 | 파일 | 크기 |
|---|---|---|
| 홈 히어로 | `/bremen/sky-pixel.webp` | `min-height: clamp(200px, 16.5vw, 470px)` |
| 히어로 장부 | `/bremen/ledger.webp` | 150px (모바일 92px) |
| 헤더 랜턴 | `/bremen/lantern.webp` | 42px, 66rem 미만에서 숨김 |
| 푸터 랜턴길 | `/bremen/lantern-road.webp` | `clamp(140px, 11.7vw, 260px)` |
| 푸터 다람쥐 | `/piatoss-squirrel.webp` | 44px, 길 위 중앙 |
| 글 끝 오너먼트 | `/bremen/ledger.webp` | 52px |
| 404 | `/wanro-cat.webp` | 96px |

- **그림을 어두운 그라디언트로 덮지 않는다.** 텍스트는 `--hero-title-shadow`·`--hero-tag-shadow`로 띄운다.
- 픽셀아트에는 `image-rendering: pixelated`를 쓴다.
- 반딧불(`.firefly`)은 히어로 4개, 랜턴길 2개까지. `prefers-reduced-motion`에서 멈춘다.
- 헤더 랜턴은 6초 주기로 흔들린다. 그 외 상시 애니메이션은 두지 않는다.

## 4. 레이아웃

- 홈: 히어로 → 최근 기록 → 티스토리 아카이브 미리보기
- 기록(`/writing`): `tistoryId`가 없는 이 블로그 고유 글만. 시간이 아니라 출처로 가르는 이름이다
- 아카이브: `tistoryId`가 있는 이관 글만 연도별로
- 조각(`/notes`): 별도 컬렉션. 목록에 전문 + 항목별 permalink. 헤더 네비에서만 접근하고 홈·RSS·sitemap에서 제외, 페이지에 `noindex`
- RSS: `tistoryId`가 없는 글만 나간다. 이관 글과 조각은 피드에 넣지 않는다
- 헤더 네비: 기록 · 조각 · 아카이브 · 카테고리 · RSS. 외부 링크는 두지 않는다
- 글 본문: 44rem 읽기 폭, 세리프 제목, 산세리프 기술 본문
- 에세이(`생각 정리`): 본문까지 Noto Serif KR과 넉넉한 행간

## 5. 타이포그래피

- 본문·네비게이션·메타데이터: Outfit(라틴) + IBM Plex Sans KR(한글)
- 제목과 에세이 본문: Noto Serif KR
- 별도 디스플레이 서체는 두지 않는다. 히어로 제목도 Outfit 800이다.
- **한글 산세리프는 400·700 굵기가 실재하는 폰트만 쓴다.** 본문 강조(`<strong>`)가 197편에 2,700곳 넘게 있어서, 굵기 하나짜리 폰트를 쓰면 강조가 통째로 사라진다. 고운돋움을 이 이유로 뺐다.
- 폰트는 자체 호스팅한다. 외부 CDN에서 받지 않는다. 구글 폰트를 쓰던 때는 `@font-face` 664개짜리 CSS(gzip 107KB)가 렌더를 막고 글 페이지 하나가 요청 51개에 710KB를 받았다 — 사이트에 실제로 쓰인 글자만 남긴 서브셋으로 5개 요청, 474KB가 됐다.
- 실제로 쓰는 웨이트만 둔다: Outfit 400·600·700·800, IBM Plex Sans KR 400·700, Noto Serif KR 400·700. 여덟 개다.

## 6. 가드레일

- 테마 토글, `data-theme`, 테마용 `localStorage`를 추가하지 않는다.
- 홈 히어로를 뷰포트 높이로 키우지 않는다.
- 기록과 이관 글을 같은 목록이나 이전·다음 탐색에 섞지 않는다.
- 조각을 글 목록·RSS·sitemap에 섞지 않는다.
- 히어로와 푸터 외에 큰 이미지 프레임을 반복하지 않는다.
- 카드와 효과보다 글 제목, 날짜, 카테고리의 탐색성을 우선한다.
- 카피는 간결하고 담백하게. 뺄 수 있으면 뺀다.
- UI 변경은 데스크탑과 모바일 실제 렌더에서 확인한 뒤 완료로 판단한다.
